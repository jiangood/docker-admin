spring:
  profiles:
    active: default,dev
  datasource:
    hikari:
      minimum-idle: 20
      maximum-pool-size: 100
  application:
    name: docker-admin
sys:
  title: 容器管理平台

logging:
  level:
    io.admin: debug
